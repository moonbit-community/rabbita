import {
  "moonbitlang/async/js_async",
  "moonbitlang/core/json",
}

import {
  "moonbitlang/core/json",
  "moonbitlang/core/debug",
} for "test"

// Imports are used only by JS declarations and tests.

warnings = "-unused_package"

options(
  targets: { "*": [ "js" ] },
)
